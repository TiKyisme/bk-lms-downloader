name: build-windows-release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install -U pip
          pip install ".[dev]"

      - name: Run tests
        run: pytest

      - name: Build EXE
        shell: pwsh
        run: |
          python -m PyInstaller `
            --noconfirm `
            --clean `
            --onefile `
            --windowed `
            --name "BK-LMS-Downloader" `
            --paths "src" `
            --collect-submodules bklms_downloader `
            --collect-all customtkinter `
            --hidden-import markdownify `
            --hidden-import pypdf `
            --hidden-import pptx `
            --collect-all selenium `
            --add-data "tools/prepare_ai_course.py;tools" `
            "app.py"

      - name: Smoke test packaged AI runtime
        shell: pwsh
        run: |
          ./dist/BK-LMS-Downloader.exe --self-test-ai
          if ($LASTEXITCODE -ne 0) { throw "Packaged AI runtime smoke failed." }

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: BK-LMS-Downloader-Windows
          path: dist/BK-LMS-Downloader.exe

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/BK-LMS-Downloader.exe
